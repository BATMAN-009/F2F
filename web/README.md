# F2F Web

Next.js 14 (App Router) + TypeScript + Tailwind.

```bash
pnpm install
pnpm dev          # http://localhost:3000
pnpm lint
pnpm test
pnpm build
```

Env: `NEXT_PUBLIC_API_BASE` (default `http://localhost:8000`).
