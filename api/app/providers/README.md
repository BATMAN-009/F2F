# Providers

Reserved for **Feature 05 — Tripo 3D Provider Integration**.

Purpose: pluggable `Provider` interface; Tripo3DProvider is the v1.0 implementation.
v1.1+ will add alternative providers behind the same interface.

Do not add code here until Feature 05 is in progress.
