# MCP — AI-Agent Addressable Engine

Reserved for **Feature 14 — MCP Server**.

Purpose: expose the F2F engine as a Model Context Protocol server so external AI
agents can drive the pipeline programmatically (generate, texture, export, query).

Do not add code here until Feature 14 is in progress.
