# Design Ledger

Reserved for **Feature 02 — Design Ledger**.

Purpose: append-only event log capturing every transformation applied to an asset
(input → mesh → texture → dimension → export). Forms the audit trail and the
source of truth for "what does this asset know about itself".

Do not add code here until Feature 02 is in progress.
