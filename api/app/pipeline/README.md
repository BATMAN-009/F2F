# Pipeline

Reserved for **Features 06–10** (input ingestion, generation, texturing,
dimensioning, export).

Purpose: stateful orchestrator that walks an asset through every Engine stage,
emitting Design Ledger events at each transition.

Do not add code here until Feature 06 is in progress.
