# F2F API

FastAPI service. See [`../docs/features/01-project-foundation/architecture.md`](../docs/features/01-project-foundation/architecture.md).

```bash
uv sync
uv run uvicorn app.main:app --reload
uv run pytest
uv run ruff check .
uv run ruff format --check .
```
